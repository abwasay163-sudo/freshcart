{
  "name": "freshcart",
  "version": "1.0.0",
  "description": "FreshCart - Premium Grocery Selling Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "shx rm -rf dist && shx mkdir -p dist && shx cp -r public/* dist/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "serverless-http": "^4.0.0"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
